export default class SpacerModel {
  positionLeft: number;
  width: number;
  color: string;

  constructor(positionLeft: number, width: number, color: string) {
    this.positionLeft = positionLeft;
    this.width = width;
    this.color = color;
  }
}
