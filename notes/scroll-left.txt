(1. normalize values:)

-100, 100, 1000
0, 200, 1100

2. determine maximum mouse position:
positionRight = positionCenter + boxWidth / 2

maxMousePos = max(positionRight of all boxes)

3. determine maximum distance at maximum zoom

maxDistance = maxMousePos * maxZoomLevel


-> The most left-positioned box will never exceed 0. 



Dynamic positioning after every zoom event.

1. get most negative position


2. normalize positions
    
    2.1 set most negative position to zero:

    

    2.2 set other positions proportionally:

